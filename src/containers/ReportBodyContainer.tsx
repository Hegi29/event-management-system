import SearchContainer from "./SearchContainer";

const ReportBodyContainer = () => {
    return (
        <SearchContainer title="report" setSelectedStatus={undefined} setSelectedSearch={undefined} />
    )
}

export default ReportBodyContainer;
