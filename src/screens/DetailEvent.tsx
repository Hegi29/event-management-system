import { ScrollView } from 'react-native-gesture-handler';

const DetailEvent: React.FunctionComponent<any> = () => {
  return (
    <ScrollView>
    </ScrollView>
  );
};

export default DetailEvent;